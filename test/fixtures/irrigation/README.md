# Irrigation fixtures

No production crop rule has been approved yet. Therefore the production fixture set is intentionally
empty and every crop calculation must return `crop_rule_unavailable`. When an agronomically approved
rule is added, this directory must receive its twenty signed validation vectors before the rule can be
activated.
